{
  "name": "built-with-tailwind-cs",
  "version": "1.0.0",
  "description": "",
  "source": "popup.html",
  "scripts": {
    "build": "rm -rf ./build && cp -R src/ build && rm -rf ./built-with-tailwind-css.zip && cd build && zip -r ../built-with-tailwind-css.zip *",
    "tailwind": "npx tailwindcss -i ./css/input.css  -o ./src/styles.css --watch --minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "postcss": "^8.4.7",
    "tailwindcss": "^3.0.23"
  }
}
